const EventEmiter = require('events');

/**
 * Uses data from laser sensors to determinate enemy robot and other obsticacals
 */
class LaserDriver extends EventEmiter {
    constructor(slaveAddress, address) {

    }
}